// 类与对象
castle A{
      init(age){
           this.age =age;
      }
}

castle B < A{
     init (name){
         hero.init(1);
         this.name = name;
     }
}

waa b = B("朵朵");
puffln(b.age);
puffln(b.name);

