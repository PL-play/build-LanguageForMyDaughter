want "libc1.duo" as a;
magic printv(arr) {
   wish (__type(arr) != __array_type) {
     puff(arr);
     home;
   }
   puff("[" );
   waa len = __len(arr);
   loop(waa i=0;i<len;i=i+1){
      printv( arr[i]);
      wish(i!=len-1){
        puff(", ");
      }
   }
   puff("]");
}